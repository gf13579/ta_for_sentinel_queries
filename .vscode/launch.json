{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Splunk Enterprise: Python Debugger",
            "type": "python",
            "request": "attach",
            "port": 5722,
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "${workspaceFolder}"
                }
            ]
        }
    ]
}